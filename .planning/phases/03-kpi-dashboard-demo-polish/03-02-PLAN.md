---
phase: 03-kpi-dashboard-demo-polish
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - axiom-ui/src/app/layout.tsx
  - axiom-ui/src/app/processor/page.tsx
  - axiom-ui/src/app/rules/page.tsx
  - axiom-ui/src/components/app-sidebar.tsx
  - axiom-ui/src/components/processor/rule-trace-panel.tsx
  - axiom-ui/src/components/processor/decision-panel.tsx
  - axiom-ui/src/components/processor/reprotection-options.tsx
  - axiom-ui/src/components/processor/audit-trail-panel.tsx
  - axiom-ui/src/components/processor/pnr-lookup-form.tsx
autonomous: false
requirements: [UI-07]

must_haves:
  truths:
    - "All page headings use consistent text-2xl font-semibold tracking-tight (no font-bold vs font-semibold inconsistency)"
    - "All dynamic content sections have animate-in fade-in transitions"
    - "Cards have subtle hover:border-primary/50 on interactive elements"
    - "Rules page shows a meaningful static display instead of 'Phase 2' placeholder text"
    - "No raw text-white or text-gray-* classes anywhere — all text uses CSS variable classes"
    - "Sidebar Metrics link highlights correctly when on /metrics route"
    - "No layout jank, broken states, or white flash on any page"
  artifacts:
    - path: "axiom-ui/src/app/rules/page.tsx"
      provides: "Meaningful rules display instead of placeholder"
      contains: "Active Rules"
    - path: "axiom-ui/src/app/processor/page.tsx"
      provides: "Polish-consistent heading style"
      contains: "font-semibold"
    - path: "axiom-ui/src/app/layout.tsx"
      provides: "Root layout with any needed transition setup"
      contains: "flex h-screen"
  key_links:
    - from: "All pages"
      to: "globals.css"
      via: "CSS variable classes only — no hardcoded colors"
      pattern: "text-foreground|text-muted-foreground|bg-card|border-border"
---

<objective>
Apply investor-demo quality polish across the entire application: consistent typography, spacing, transitions, hover states, and brand alignment.

Purpose: Ensure the application meets the "investor-demo quality" bar required by UI-07 — no visual inconsistencies, no broken states, professional polish throughout.
Output: All pages visually consistent with AXIOM brand guidelines. Rules page updated from placeholder to meaningful static display. Every interactive element has appropriate hover/transition feedback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-kpi-dashboard-demo-polish/03-RESEARCH.md
@.planning/phases/03-kpi-dashboard-demo-polish/03-01-SUMMARY.md

@axiom-ui/src/app/layout.tsx
@axiom-ui/src/app/globals.css
@axiom-ui/src/app/processor/page.tsx
@axiom-ui/src/app/rules/page.tsx
@axiom-ui/src/app/metrics/page.tsx
@axiom-ui/src/components/app-sidebar.tsx
@axiom-ui/src/components/processor/rule-trace-panel.tsx
@axiom-ui/src/components/processor/decision-panel.tsx
@axiom-ui/src/components/processor/reprotection-options.tsx
@axiom-ui/src/components/processor/audit-trail-panel.tsx
@axiom-ui/src/components/processor/pnr-lookup-form.tsx

<interfaces>
From axiom-ui/src/app/layout.tsx:
```typescript
// Root layout wraps all pages with ThemeProvider, QueryProvider, AppSidebar
// Main content: <main className="flex-1 overflow-y-auto p-6">
```

From axiom-ui/src/components/app-sidebar.tsx:
```typescript
const navigation = [
  { name: "Processor", href: "/processor", icon: Search },
  { name: "Rules", href: "/rules", icon: BookOpen },
  { name: "Metrics", href: "/metrics", icon: BarChart3 },
];
// Active state: pathname === item.href || pathname.startsWith(item.href + "/")
```

Brand consistency reference (from 03-RESEARCH.md):
- Primary color: oklch(0.72 0.15 185) (teal) — via text-primary, bg-primary
- Card background: oklch(0.18 0.02 260) — via bg-card
- Border color: oklch(0.28 0.02 260) — via border-border
- Primary text: oklch(0.95 0.01 260) — via text-foreground
- Muted text: oklch(0.6 0.01 260) — via text-muted-foreground
- Spacing: space-y-6 for page sections, gap-4 for card grids, gap-6 for chart grids
- Border radius: rounded-lg (0.5rem) for cards
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Brand polish pass — typography, transitions, hover states, rules page, consistency audit</name>
  <files>
    axiom-ui/src/app/processor/page.tsx
    axiom-ui/src/app/rules/page.tsx
    axiom-ui/src/components/app-sidebar.tsx
    axiom-ui/src/components/processor/rule-trace-panel.tsx
    axiom-ui/src/components/processor/decision-panel.tsx
    axiom-ui/src/components/processor/reprotection-options.tsx
    axiom-ui/src/components/processor/audit-trail-panel.tsx
    axiom-ui/src/components/processor/pnr-lookup-form.tsx
  </files>
  <read_first>
    axiom-ui/src/app/processor/page.tsx
    axiom-ui/src/app/rules/page.tsx
    axiom-ui/src/app/metrics/page.tsx
    axiom-ui/src/components/app-sidebar.tsx
    axiom-ui/src/components/processor/rule-trace-panel.tsx
    axiom-ui/src/components/processor/decision-panel.tsx
    axiom-ui/src/components/processor/reprotection-options.tsx
    axiom-ui/src/components/processor/audit-trail-panel.tsx
    axiom-ui/src/components/processor/pnr-lookup-form.tsx
    axiom-ui/src/components/processor/flight-option-card.tsx
    axiom-ui/src/app/globals.css
  </read_first>
  <action>
    Polish checklist — read each file, apply ALL applicable items. This is a consistency pass, not a rewrite. Make targeted edits only.

    **A. Typography consistency** (per UI-07):
    Ensure ALL page headings use the same pattern:
    - h1: `text-2xl font-semibold tracking-tight` (processor page already uses this)
    - Page description: `text-sm text-muted-foreground`
    - Rules page heading must match processor and metrics heading style exactly
    - If metrics page from Plan 01 uses font-bold instead of font-semibold, change to font-semibold
    - Check: No `font-bold` on any h1 element. Every h1 must be font-semibold.

    **B. Animation consistency**:
    Ensure ALL dynamic content sections use `animate-in fade-in duration-200`:
    - Check processor page: ReservationPanel, RuleTracePanel+DecisionPanel grid, ReprotectionOptions, AuditTrailPanel — each should have animate-in fade-in
    - Check metrics page: stat cards grid, charts grid — should have animate-in fade-in
    - If any section is missing the animation class, add it to the container div

    **C. Hover states on interactive cards**:
    - Flight option cards (in reprotection-options or flight-option-card): add `hover:border-primary/50 transition-colors` if not present
    - StatCard (already done in Plan 01, verify)
    - PNR lookup form Card: add `hover:border-primary/30 transition-colors` for subtle feedback
    - Do NOT add hover states to non-interactive display cards (reservation panel, rule trace) — they are read-only

    **D. Rules page update** — replace placeholder with meaningful static display:
    Replace the "Phase 2" placeholder with a static rules summary. Use the same page heading pattern. Content:

    ```tsx
    "use client";

    import { useQuery } from "@tanstack/react-query";
    import { apiFetch } from "@/lib/api";
    import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
    import { Badge } from "@/components/ui/badge";
    import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
    import { Skeleton } from "@/components/ui/skeleton";
    import { BookOpen } from "lucide-react";
    ```

    Fetch rules from GET /api/rules (already implemented in backend). Display as a table inside a Card:
    - Columns: Field, Operator, Value, Action, Priority, Status (active badge)
    - Use useQuery with queryKey ["rules"] and queryFn calling `apiFetch<RuleSchema[]>("/rules")`
    - Define a minimal RuleSchema interface inline (or add to types.ts): `{ id: number; field: string; operator: string; value: string; action: string; priority: number; active: boolean }`
    - Loading state: table skeleton
    - Active column: Badge variant "approved" for active, "default" for inactive
    - Humanize field names: replace underscores with spaces, capitalize first letter
    - Page header: "Active Rules" with description "Business rules governing the decision engine."
    - Add a count badge next to the title showing total rules count

    This gives the Rules page real data from the API instead of a placeholder, making the demo more complete. The investor sees rules exist and are configurable. No CRUD — just read-only display.

    **E. Color consistency audit**:
    - Grep all processor components for `text-white`, `text-gray-`, `bg-gray-`, `border-gray-` or any hardcoded hex values
    - Replace any found instances with CSS variable equivalents: text-foreground, text-muted-foreground, bg-card, border-border, etc.
    - Exception: Tailwind opacity modifiers like `/50`, `/30` are fine on CSS variable classes

    **F. Sidebar verification**:
    - Confirm the sidebar pathname matching in app-sidebar.tsx correctly highlights "Metrics" when on /metrics
    - The current code uses `pathname === item.href || pathname.startsWith(item.href + "/")` which should work for /metrics
    - If any issues found, fix. Otherwise leave as-is.

    **G. Spacing consistency**:
    - Verify all pages use `space-y-6` for top-level section spacing
    - Verify card grids use `gap-4` for stat card grids and `gap-6` for chart/panel grids
    - Fix any inconsistencies found
  </action>
  <verify>
    <automated>cd /Users/fbetncourtc/Documents/finops/axion-airflight-engine/axiom-ui && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - rules/page.tsx contains "Active Rules"
    - rules/page.tsx contains "useQuery"
    - rules/page.tsx contains "apiFetch"
    - rules/page.tsx does NOT contain "Phase 2"
    - processor/page.tsx contains "font-semibold" on h1
    - processor/page.tsx does NOT contain "font-bold" on h1
    - No files in src/ contain "text-white" (except if in a comment)
    - No files in src/ contain "text-gray-" (except if in a comment)
    - npx tsc --noEmit exits 0
    - npm run build succeeds
  </acceptance_criteria>
  <done>All pages have consistent typography (font-semibold headings), animation transitions on dynamic content, hover states on interactive cards. Rules page shows live rule data from API instead of placeholder. No hardcoded colors — all CSS variable classes. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete application polish</name>
  <files>none — visual verification only</files>
  <action>
    Human visually verifies the entire application across all three pages (Metrics, Rules, Processor) for investor-demo quality. No code changes — this is a verification gate.
  </action>
  <verify>
    <automated>cd /Users/fbetncourtc/Documents/finops/axion-airflight-engine/axiom-ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <what-built>
    Complete KPI dashboard (Plan 01) and brand polish pass (Task 1 above). The entire application should now be investor-demo ready:
    - Metrics page: 4 stat cards + 2 charts + status distribution
    - Rules page: Live rules table from API
    - Processor page: Full workflow with consistent styling
    - All pages: Consistent headings, animations, hover states, dark theme
  </what-built>
  <how-to-verify>
    Prerequisites: Backend running (`uvicorn axiom.api.main:app --port 8000`), seed data loaded (`python -m axiom.db.seed`), frontend running (`cd axiom-ui && pnpm dev`).

    1. **Metrics page** (http://localhost:3000/metrics):
       - See 4 stat cards: Total Decisions (~87), Automation Rate (~87.4%), Avg Processing Time (~42ms), Recent Day count
       - See stacked area chart with 14 days of data, teal/amber/red areas
       - See horizontal bar chart with 4 rules (Involuntary Change highest)
       - See status distribution section at bottom
       - Hover over a stat card — subtle border color change
       - Hover over chart data points — dark-themed tooltip appears (not white)
       - Wait 30+ seconds — data should silently refetch

    2. **Rules page** (http://localhost:3000/rules):
       - See "Active Rules" heading with rule count badge
       - See table with rule data (field, operator, value, action, priority, status)
       - Active rules show green badge, inactive show default

    3. **Processor page** (http://localhost:3000/processor):
       - Enter a valid seed PNR and last name
       - Skeleton loads, then reservation + rule trace + decision appear with fade-in animation
       - If approved, reprotection options appear with hover states on flight cards
       - Complete a selection — audit trail appears with fade-in

    4. **Cross-page consistency**:
       - All three pages have matching heading style (same weight, size, tracking)
       - Sidebar highlights correct page on each navigation
       - No white flash when navigating between pages
       - No layout jank or content jumps on any page
       - All text is readable — no overly faint or invisible text

    5. **Brand alignment**:
       - Teal accent visible on every page (sidebar active, chart colors, buttons)
       - Dark background consistent everywhere
       - Card borders subtle and consistent
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues to fix</resume-signal>
  <done>Human has verified all three pages meet investor-demo quality bar. No visual inconsistencies, no broken states, brand-aligned throughout.</done>
</task>

</tasks>

<verification>
1. `cd axiom-ui && npx tsc --noEmit` — Zero TypeScript errors
2. `cd axiom-ui && npm run build` — Build succeeds
3. Visual verification of all three pages per checkpoint task above
4. Grep check: no `text-white` or `text-gray-*` in source files
5. Grep check: no `font-bold` on h1 elements
</verification>

<success_criteria>
- All three pages (Processor, Rules, Metrics) have consistent investor-demo quality polish
- No placeholder text remains ("Phase 2", "Phase 3")
- Typography, spacing, animations, hover states are uniform across pages
- Brand teal accent visible on every page
- Human verifier approves the visual quality
</success_criteria>

<output>
After completion, create `.planning/phases/03-kpi-dashboard-demo-polish/03-02-SUMMARY.md`
</output>
